async function reviewFormHandler(event) {
    event.preventDefault();

    const movieTitle = document.querySelector('#movie-title').value.trim();
    const movieYear = document.querySelector('#movie-year').value;
    const movieDescription = documnet.querySelector('#movie-description').value;

    // search.js from MovieApp is the model for this
};